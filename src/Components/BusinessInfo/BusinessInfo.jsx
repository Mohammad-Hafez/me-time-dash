import React from 'react';
import styles from './BusinessInfo.module.css';

const BusinessInfo = ({t}) => {
    return <>
        <div className={styles.businessInfo}>

        </div>
    </>
};

export default BusinessInfo;
